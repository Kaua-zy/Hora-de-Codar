programa {
  funcao inicio() {
    real basemaior , basemenor, h
 escreva("basemaior\n")
 leia (basemaior)
 escreva("basemenor")
leia(basemenor) 
 escreva ("altura")
 leia (h)
 real resultado = (basemaior + basemenor) * h / 2
  escreva("o resultado de basemaior + basemenor * altura / 2 "+ resultado)
  
  
 
  }
}
